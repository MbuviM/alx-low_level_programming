#include "main.h"
#include <stdio.h>

/**
 * _strlen - Function to print the length of a string.
 * @s: Pointer parameter variable.
 * Return: 0 Success.
*/

int _strlen(char *s)
{
	putchar(strlen(*s));
	return (0);
}
