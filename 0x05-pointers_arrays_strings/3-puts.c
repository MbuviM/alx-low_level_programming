#include "main.h"
#include <stdio.h>
/**
 * _puts - Functions that prints the string
 * @str:  pointer parameter variable.
 * Return: (0) Success.
*/

void _puts(char *str)
{
	puts(str);
	puts('\n');
}
